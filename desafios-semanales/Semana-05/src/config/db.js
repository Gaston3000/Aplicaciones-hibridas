// Configuración de base de datos (placeholder).
// Más adelante se reemplazará con la conexión real (Mongo/Mongoose).

export const connectDB = async () => {
    console.log('DB conectada (stub)');
};

export default connectDB;
