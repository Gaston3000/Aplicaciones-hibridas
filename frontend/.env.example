# Copiar este archivo como ".env" (no se sube al repositorio).

# URL del backend.
# - Desarrollo local: http://localhost:3000
# - Producción: la URL pública de la API desplegada (por ejemplo en Render)
VITE_API_URL=http://localhost:3000
