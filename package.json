{
  "name": "limpieza-total",
  "version": "1.0.0",
  "private": true,
  "description": "Tienda de productos de limpieza. Backend con Express + MongoDB + JWT y frontend con React.",
  "scripts": {
    "setup": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently -n backend,frontend -c blue,green \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "author": "Gaston",
  "license": "ISC"
}
